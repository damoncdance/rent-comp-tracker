1. **Edit 004 in place.** Given production has no `schema_migrations` ledger yet, the runner will apply the file currently present on `main`, in filename order, and then record that exact version ([migrations/runner.py](/Users/damondance/Documents/Code-Projects/rent-comp-tracker/migrations/runner.py:67), [migrations/runner.py](/Users/damondance/Documents/Code-Projects/rent-comp-tracker/migrations/runner.py:70), [migrations/runner.py](/Users/damondance/Documents/Code-Projects/rent-comp-tracker/migrations/runner.py:85)). Since 004 has not run in prod, correcting the source migration avoids ever creating the wrong `455-carpenter` row there.

Risk of edit-in-place: any non-prod DB that already recorded `004_add_455_carpenter` will skip the corrected 004, so those DBs keep `455-carpenter` unless manually fixed/reset ([migrations/runner.py](/Users/damondance/Documents/Code-Projects/rent-comp-tracker/migrations/runner.py:71)). That matches your local/throwaway situation and does not matter for prod.

Risk of adding 005: production would run bad 004 first, inserting `455-carpenter` because `slug` is the unique/ignore key ([migrations/004_add_455_carpenter.py](/Users/damondance/Documents/Code-Projects/rent-comp-tracker/migrations/004_add_455_carpenter.py:28), [schema.sql](/Users/damondance/Documents/Code-Projects/rent-comp-tracker/schema.sql:7)), then 005 would have to update it. That works, but it preserves a known-wrong migration and creates needless history for something never deployed.

2. The old slug can only linger where 004 already ran: your local dev DB, throwaway DBs, or any collaborator DB that applied pushed 004 before the fix. It should not linger in production if 004 is edited before the first prod migration run. Also update 004’s docstring and post-insert lookup/print, not just the INSERT values, because those are hard-coded to `455-carpenter` too ([migrations/004_add_455_carpenter.py](/Users/damondance/Documents/Code-Projects/rent-comp-tracker/migrations/004_add_455_carpenter.py:1), [migrations/004_add_455_carpenter.py](/Users/damondance/Documents/Code-Projects/rent-comp-tracker/migrations/004_add_455_carpenter.py:52)).

3. Keep `active = 0`. Daily runs only `active = 1` properties ([src/config.py](/Users/damondance/Documents/Code-Projects/rent-comp-tracker/src/config.py:31), [src/daily_run.py](/Users/damondance/Documents/Code-Projects/rent-comp-tracker/src/daily_run.py:45)), so no fetch happens while inactive. Minimal optional hardening: change `platform` from placeholder `"rentcafe"` to an unsupported honest value like `"wix"` or `"unknown"` so an accidental `active=1` flip fails before fetching/parsing ([src/daily_run.py](/Users/damondance/Documents/Code-Projects/rent-comp-tracker/src/daily_run.py:92), [src/parsers/__init__.py](/Users/damondance/Documents/Code-Projects/rent-comp-tracker/src/parsers/__init__.py:53)). But if you want the smallest possible correction, leave platform alone and only fix `455` → `465` plus the migration text/lookup.
